FROM python:3.10-slim

WORKDIR /app

COPY requirements-client.txt .
RUN pip install --no-cache-dir -r requirements-client.txt

COPY client_agent.py .
COPY prod_app.py .

EXPOSE 8501

CMD ["streamlit", "run", "prod_app.py", "--server.port=8501", "--server.address=0.0.0.0"]
